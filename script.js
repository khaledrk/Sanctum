const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];const header=$('.site-header');let lastY=0;addEventListener('scroll',()=>{const y=scrollY;if(header)header.style.transform=y>lastY&&y>150?'translateY(-110%)':'translateY(0)';lastY=y},{passive:true});const toggle=$('.menu-toggle'),nav=$('#site-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});const page=document.body.dataset.page;$$('#site-nav a').forEach(a=>{if(a.getAttribute('href')?.startsWith(page))a.classList.add('active')});const cursor=$('.cursor');addEventListener('pointermove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});$$('a,button').forEach(el=>{el.addEventListener('pointerenter',()=>cursor?.classList.add('active'));el.addEventListener('pointerleave',()=>cursor?.classList.remove('active'))});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08});$$('.panel>*,.chapter>*,.drawing-chapter>*,.reference-grid article').forEach(el=>{el.classList.add('reveal');io.observe(el)});const canvas=$('#spore-field');if(canvas&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const ctx=canvas.getContext('2d');let w,h,pts=[];function size(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;pts=Array.from({length:Math.min(70,innerWidth/18)},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.4+.2,v:(Math.random()*.16+.03)}))}function tick(){ctx.clearRect(0,0,w,h);ctx.fillStyle='rgba(200,211,111,.55)';for(const p of pts){p.y-=p.v;if(p.y<0)p.y=h;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,7);ctx.fill()}requestAnimationFrame(tick)}size();addEventListener('resize',size);tick()}$$('[data-call]').forEach(b=>b.addEventListener('click',()=>{const line=$('[data-call-line]');if(line)line.textContent=b.dataset.call;$$('[data-call]').forEach(x=>x.classList.toggle('active',x===b))}));$$('[data-filter]').forEach(b=>b.addEventListener('click',()=>{const f=b.dataset.filter;$$('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));$$('.reference-grid article').forEach(a=>a.classList.toggle('hide',f!=='all'&&a.dataset.type!==f))}));$$('.zoomable').forEach(a=>{a.setAttribute('target','_blank');a.setAttribute('rel','noopener')});const prelude=$('.prelude'),storm=$('#storm');$$('[data-sound]').forEach(b=>b.addEventListener('click',async()=>{if(b.dataset.sound==='on'&&storm){storm.volume=.16;try{await storm.play()}catch{}}prelude?.classList.add('done')}));if(prelude)setTimeout(()=>{if(!prelude.classList.contains('done'))prelude.querySelector('.sound-entry')?.classList.add('ready')},700);

