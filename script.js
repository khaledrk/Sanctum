const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];
const header=$('.site-header');let lastY=0;addEventListener('scroll',()=>{const y=scrollY;if(header)header.style.transform=y>lastY&&y>130?'translateY(-110%)':'translateY(0)';lastY=y},{passive:true});
const prelude=$('.prelude');addEventListener('load',()=>setTimeout(()=>prelude?.classList.add('done'),900));
const toggle=$('.menu-toggle'),nav=$('#site-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open);document.body.classList.toggle('menu-open',open)});
const page=document.body.dataset.page;$$('#site-nav a').forEach(a=>{if(a.getAttribute('href')?.startsWith(page))a.classList.add('active')});
const cursor=$('.cursor');addEventListener('pointermove',e=>{if(!cursor)return;cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});$$('a,button,.system-node').forEach(el=>{el.addEventListener('pointerenter',()=>cursor?.classList.add('active'));el.addEventListener('pointerleave',()=>cursor?.classList.remove('active'))});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});$$('.section>*,.chapter>*,.fieldnote-card,.principle-list article').forEach(el=>{el.classList.add('reveal');io.observe(el)});
const canvas=$('#spore-field');if(canvas){const ctx=canvas.getContext('2d');let w,h,dpr,pts=[];function size(){dpr=Math.min(devicePixelRatio||1,2);w=canvas.width=innerWidth*dpr;h=canvas.height=innerHeight*dpr;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';pts=Array.from({length:Math.min(90,Math.floor(innerWidth/14))},()=>({x:Math.random()*w,y:Math.random()*h,r:(Math.random()*1.6+.3)*dpr,vx:(Math.random()-.5)*.12*dpr,vy:(Math.random()*.2+.04)*dpr}))}function tick(){ctx.clearRect(0,0,w,h);ctx.fillStyle='rgba(200,214,107,.65)';for(const p of pts){p.x+=p.vx;p.y-=p.vy;if(p.y<0)p.y=h;if(p.x<0)p.x=w;if(p.x>w)p.x=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(tick)}size();addEventListener('resize',size);if(!matchMedia('(prefers-reduced-motion: reduce)').matches)tick()}
const orbit=$('#system-orbit');if(orbit){orbit.addEventListener('pointermove',e=>{const r=orbit.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;$$('.system-node',orbit).forEach((n,i)=>n.style.translate=`${x*(i%2?10:-10)}px ${y*(i%3?8:-8)}px`)})}
$$('.figure-link').forEach(a=>a.setAttribute('target','_blank'));